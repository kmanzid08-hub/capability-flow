# Infrastructure

Local PostgreSQL is defined in the root `docker-compose.yml`. Deployment manifests, secret-store integration, observability, backups, and production networking belong here in later milestones.
